module.exports = {
  content: [
    './src/cpm_odoo/static/src/backend/**/*.{html,js,ts,jsx,tsx,xml}', // Đường dẫn tới các tệp trong backend
  ],
  theme: {
    extend: {
      // Bạn có thể thêm các tùy chỉnh ở đây
    },
  },
  plugins: [
    // Thêm plugin nếu cần
  ],
}